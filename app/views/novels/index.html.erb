<h1>成為小說家吧</h1>
<ul>
    <% @novels.each do |n| %>
    <li> <%= link_to n.title, novel_path(n.id) %> </li>
    <% end %>
</ul>

<%= if user_signed_in? %>
    <%= link_to "建立新的小說", new_novel_path %>
<%= else %>
    <%= link_to "登出", destroy_user_session, method: :delete %>
<%= end %>